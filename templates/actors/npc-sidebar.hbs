<div class="sidebar">

    {{!-- HD --}}
    {{#if important}}
    {{#with system.attributes.hd}}
    <div class="pills-group empty">
        {{!-- TODO: Allow clicking this to roll a hit die --}}
        <h3 class="icon">
            <i class="fa-solid fa-dice-d{{ denomination }}"></i>
            <span class="roboto-upper">{{ localize "BLEACHD20.HitDice" }}</span>
            <span class="counter">{{ value }} &sol; {{ max }}</span>
        </h3>
    </div>
    {{/with}}
    {{/if}}

    {{!-- Speed --}}
    {{> "bleachd20.actor-trait-pills" values=speed key="speed" icon="fas fa-shoe-prints"
        label="BLEACHD20.Speed" configAction="movement" }}

    {{!-- Skills --}}
    <div class="pills-group {{#unless skills}}empty{{/unless}}">
        <h3 class="icon">
            <i class="fas fa-briefcase" inert></i>
            <span class="roboto-upper">{{ localize "BLEACHD20.Skills" }}</span>
            {{#if editable}}
            <button type="button" class="config-button unbutton" data-action="showConfiguration" data-config="skills"
                    data-tooltip="BLEACHD20.SkillsConfig" aria-label="{{ localize 'BLEACHD20.SkillsConfig' }}">
                <i class="fas fa-cog" inert></i>
            </button>
            {{/if}}
        </h3>
        <ul class="pills">
            {{#each skills}}
            <li class="pill">
                <a class="{{ @root.rollableClass }} skill-name" data-key="{{ key }}"
                   data-action="roll" data-type="skill">
                    <span class="label">{{ label }}</span>
                    <span class="separator">&vert;</span>
                    <span class="value">{{ bleachd20-numberFormat total signDisplay="always" }}</span>
                </a>
            </li>
            {{/each}}
        </ul>
    </div>

    {{!-- Senses --}}
    {{> "bleachd20.actor-trait-pills" values=senses key="senses" icon="fas fa-eye"
        label="BLEACHD20.Senses" configAction="senses" }}

    {{!-- Resistances --}}
    {{> "bleachd20.actor-trait-pills" values=traits.dr key="dr" color="green" icon="fas fa-shield-halved"
        label="BLEACHD20.Resistances" }}

    {{!-- Immunities --}}
    {{#if editable}}
        {{> "bleachd20.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="BLEACHD20.DamImm" }}
    {{else}}
        {{> "bleachd20.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="BLEACHD20.Immunities" }}
    {{/if}}
    {{> "bleachd20.actor-trait-pills" values=traits.ci key="ci" color="green" svg="rosa-shield"
        label="BLEACHD20.ConImm" }}

    {{!-- Vulnerabilities --}}
    {{> "bleachd20.actor-trait-pills" values=traits.dv key="dv" color="maroon" icon="fas fa-heart-crack"
        label="BLEACHD20.Vulnerabilities" }}

    {{!-- Damage Modification --}}
    {{> "bleachd20.actor-trait-pills" values=traits.dm key="dm" icon="fas fa-heart-circle-plus"
        label="BLEACHD20.DamageModification.Label" }}

    {{!-- Languages --}}
    {{> "bleachd20.actor-trait-pills" values=traits.languages key="languages" icon="fas fa-flag"
        label="BLEACHD20.Languages" }}

    {{!-- Habitat --}}
    {{> "bleachd20.actor-trait-pills" values=habitat configAction="habitat" icon="fa-solid fa-mountain-sun"
        label="BLEACHD20.Habitat.Configuration.Label" }}

    {{!-- Treasure --}}
    {{!-- TODO: Allow clicking a treasure category pill to roll appropriately for random treasure --}}
    {{> "bleachd20.actor-trait-pills" values=treasure configAction="treasure" icon="fa-solid fa-gem"
        label="BLEACHD20.Treasure.Configuration.Label" }}

</div>
