<section class="flexcol">
    <div class="note info">
        {{ localize (ifThen isGroup "BLEACHD20.REST.Long.Hint.Group" "BLEACHD20.REST.Long.Hint.Normal") }}
    </div>

    {{#if fields.length}}
    <fieldset>
        <legend>{{ localize "BLEACHD20.REST.Configuration" }}</legend>
        {{#each fields}}
        {{ formField field name=name value=value input=input options=options rootId=../partId }}
        {{/each}}
    </fieldset>
    {{/if}}
</section>
