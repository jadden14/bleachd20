<dialog class="warnings locked-tooltip bleachd20-tooltip active">
    <ul class="unlist">
        {{#each warnings}}
        <li class="{{ type }}">
            {{#if link}}<a data-action="inspectWarning" data-target="{{ link }}">{{/if}}
            {{ message }}
            {{#if link}}</a>{{/if}}
        </li>
        {{/each}}
    </ul>
</dialog>
