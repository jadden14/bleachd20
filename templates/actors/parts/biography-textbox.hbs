<div class="textbox {{ class }}">
    <h3 class="icon">
        {{#if icon}}<i class="{{ icon }}"></i>
        {{~else if svg}}<bleachd20-icon src="systems/bleachd20/icons/svg/{{ svg }}.svg"></bleachd20-icon>{{/if}}
        <span class="roboto-upper">{{ localize label }}</span>
    </h3>
    {{#if @root.editable}}
    <textarea class="untext" name="{{ name }}">{{ value }}</textarea>
    {{else}}
    <p>{{ bleachd20-textFormat value }}</p>
    {{/if}}
</div>
