<div class="bleachd202 chat-card request-card">
    <div class="card-buttons">
        <button {{bleachd20-dataset dataset}}>
            <span class="visible-dc">{{{buttonLabel}}}</span>
            <span class="hidden-dc">{{{hiddenLabel}}}</span>
        </button>
    </div>
</div>
