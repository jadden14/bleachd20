<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "BLEACHD20.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "bleachd20.activity-usage-notes" }}
    {{/if~}}
</section>
