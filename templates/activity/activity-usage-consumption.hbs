<section>
    {{~#if hasConsumption}}
    <fieldset>
        <legend>{{ localize "BLEACHD20.USAGE.SECTION.Consumption" }}</legend>
        {{#each fields}}
        <div class="form-group">
            <label>
                {{ field.label }}
                {{#if warn}}<i class="fa-solid fa-triangle-exclamation warning"></i>{{/if}}
            </label>
            <div class="form-fields">
                {{ formInput field name=name value=value input=input }}
            </div>
            {{#if field.hint}}<p class="hint">{{{ field.hint }}}{{/if}}
        </div>
        {{/each}}
    </fieldset>
    {{> "bleachd20.activity-usage-notes" }}
    {{/if~}}
</section>
