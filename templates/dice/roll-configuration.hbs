<fieldset>
    <legend>{{ localize "BLEACHD20.RollConfiguration.Configuration" }}</legend>
    {{#each fields}}
        {{ formField field name=name value=value options=options input=input }}
    {{/each}}
</fieldset>
