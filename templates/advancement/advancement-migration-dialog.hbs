<fieldset class="card items">
    <legend>{{ localize "BLEACHD20.ADVANCEMENT.Migration.Selection" }}</legend>
    <div class="form-group hint">
        <p class="hint">{{ localize "BLEACHD20.ADVANCEMENT.Migration.Hint" name=item.name }}</p>
    </div>
    <div class="header">
        <div class="item-name">{{ localize "BLEACHD20.AdvancementTitle" }}</div>
        <div class="item-controls">{{ localize "BLEACHD20.Add" }}</div>
    </div>
    <ol class="items-list unlist">
        {{#each advancements}}
        <li class="advancement-item item flexrow">
            <div class="item-name flexrow">
                {{#if svg}}
                <bleachd20-icon src="{{ icon }}"></bleachd20-icon>
                {{else}}
                <img class="gold-icon" src="{{ icon }}" alt="{{ title }}">
                {{/if}}
                <span class="name">{{ title }}</span>
            </div>
            <div class="item-controls flexrow">
                <bleachd20-checkbox name="{{ id }}" checked></bleachd20-checkbox>
            </div>
            {{#if summary}}
            <div class="item-summary">
                {{{ summary }}}
            </div>
            {{/if}}
        </li>
        {{/each}}
    </ol>
</fieldset>
