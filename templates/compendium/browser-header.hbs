<div>
    <div class="mode-toggle">
        {{#if showModeToggle}}
        <slide-toggle class="mode-slider header-control" data-action="toggleMode" {{ checked isAdvanced }}
                      id="{{ partId }}-mode-toggle"></slide-toggle>
        <label for="{{ partId }}-mode-toggle">{{ localize "BLEACHD20.Advanced" }}</label>
        {{/if}}
    </div>
    <h2>{{ localize "BLEACHD20.CompendiumBrowser.Title" }}</h2>
</div>
