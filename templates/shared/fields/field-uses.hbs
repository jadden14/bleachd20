<fieldset>
    <legend>{{ localize "BLEACHD20.Usage" }}</legend>

    {{!-- Uses --}}
    <div class="form-group split-group">
        <label>{{ localize "BLEACHD20.LimitedUses" }}</label>
        <div class="form-fields">

            {{!-- Spent --}}
            {{ formField fields.uses.fields.spent value=data.uses.spent label="BLEACHD20.Spent" localize=true hint=false
                         classes="label-top" }}

            {{!-- Max --}}
            {{ formField fields.uses.fields.max value=data.uses.max label="BLEACHD20.Max" localize=true hint=false
                         classes="label-top" }}
        </div>
    </div>

    {{!-- Auto-Destroy --}}
    {{#if fields.uses.fields.autoDestroy}}
    {{ formField fields.uses.fields.autoDestroy value=data.uses.autoDestroy localize=true
                 input=inputs.createCheckboxInput }}
    {{/if}}
</fieldset>

{{#if hasLimitedUses}}
<fieldset>
    <legend>
        <span>{{ localize "BLEACHD20.Recovery" }}</span>
        <button type="button" class="unbutton control-button" data-action="addRecovery"
                data-tooltip aria-label="{{ localize "BLEACHD20.USES.Recovery.Action.Create" }}">
            <i class="fas fa-plus" inert></i>
        </button>
    </legend>
    {{#each usesRecovery}}
    <div class="form-group split-group full-width card" data-index="{{ @index }}">
        <div class="form-fields">

            {{!-- Period --}}
            {{ formField fields.period name=(concat prefix "period") value=source.period options=@root.recoveryPeriods
                         label="BLEACHD20.Period" localize=true hint=false classes="label-top" }}

            {{!-- Type --}}
            {{#unless (eq source.period "recharge")}}
            {{ formField fields.type name=(concat prefix "type") value=source.type options=@root.recoveryTypes
                         label="BLEACHD20.Recovery" localize=true hint=false classes="label-top" }}
            {{/unless}}

            {{!-- Formula --}}
            {{#if (or (eq source.type "formula") formulaOptions)}}
            {{ formField fields.formula name=(concat prefix "formula") value=source.formula options=formulaOptions
                         label="BLEACHD20.Formula" localize=true hint=false classes="label-top" }}
            {{/if}}

            <button type="button" class="unbutton control-button" data-action="deleteRecovery"
                    data-tooltip aria-label="{{ localize "BLEACHD20.USES.Recovery.Action.Delete" }}">
                <i class="fas fa-minus" inert></i>
            </button>
        </div>
    </div>
    {{else}}
    <div class="empty">{{ localize "BLEACHD20.USES.Recovery.Never" }}</div>
    {{/each}}
</fieldset>
{{/if}}
